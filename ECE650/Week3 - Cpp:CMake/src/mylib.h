#ifndef SINGLE_LIST_H
#define SINGLE_LIST_H

//C++用头文件保护（include guard）防止重复定义，Python自动避免重复导入模块。

// 头文件内容
template <typename Type>
class Single_list {
    // ...
};

#endif


//* Python的模块（.py文件）通过import语句导入。
// Python解释器会自动避免同一个模块被多次导入。
// 当你第一次import xxx时，Python会加载并执行该模块，并在sys.modules中做个记录。
// 以后再import xxx，Python会直接用已经加载好的模块，不会重复执行。
// */


